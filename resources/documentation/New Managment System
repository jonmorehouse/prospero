New Property

1.) Create a new row for every single category so we don't have problems switching in the future
2.) Update thumbnails -- with the default -- which is set already though in table_schema
